apiVersion: apps/v1
kind: Deployment
metadata:
  name: catalogues-mongo-depl
spec:
  replicas: 1
  selector:
    matchLabels:
      app: catalogues-mongo
  template:
    metadata:
      labels:
        app: catalogues-mongo
    spec:
      containers:
        - name: catalogues-mongo
          image: mongo
---
apiVersion: v1
kind: Service
metadata:
  name: catalogues-mongo-srv
spec:
  selector:
    app: catalogues-mongo
  ports:
    - name: db
      protocol: TCP
      port: 27017
      targetPort: 27017
